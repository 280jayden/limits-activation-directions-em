# Copy to .env (or export in your shell). The original notebooks read these from Colab Secrets.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
# GPT-4o judge is called through OpenRouter
OPENROUTER_API_KEY=
# Only needed by a couple of exploratory scripts that pull gated HF models
HF_TOKEN=
# Optional: point the code at your own bucket
EM_S3_BUCKET=jayden-algoverse-sp26
